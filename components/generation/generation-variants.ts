export const containerClasses = {
  default: "rounded-2xl hover:bg-[#1D2125]",
  compact: "rounded-xl hover:bg-hover",
};

export const innerContainerClasses = {
  default: "px-2 py-2.5 h-20",
  compact: "p-2 h-16",
};

export const thumbnailContainerClasses = {
  default: "w-15 h-15 rounded-2xl",
  compact: "w-12 h-12 rounded-lg",
};

export const thumbnailInnerClasses = {
  default: "rounded-2xl",
  compact: "rounded-lg",
};

export const titleClasses = {
  default: "text-base mb-1",
  compact: "text-sm mb-0.5",
};

export const descriptionClasses = {
  default: "text-sm pr-40 whitespace-nowrap overflow-hidden",
  compact: "text-xs truncate",
};

export const playIconClasses = {
  default: "w-4 h-4",
  compact: "w-4 h-4",
};

export const progressTextClasses = {
  default: "text-xs",
  compact: "text-xs",
};

export const badgeClasses = {
  default: "px-3 py-1 rounded-[0.625rem] text-sm",
  compact: "px-2 py-0.5 rounded-lg text-xs",
};

export const actionButtonClasses = {
  default: "p-2.5",
  compact: "p-1.5",
};

export const actionIconSize = {
  default: { width: 20, height: 20 },
  compact: { width: 16, height: 16 },
};

export const moreIconClasses = {
  default: "w-5 h-5",
  compact: "w-4 h-4",
};

export const actionGapClasses = {
  default: "gap-2",
  compact: "gap-1",
};

export const gradientClasses = {
  default: "gap-4 -left-16 from-[#1D2125] via-[#1D2125]/90",
  compact: "gap-3 -left-12 from-hover via-hover/90",
};
